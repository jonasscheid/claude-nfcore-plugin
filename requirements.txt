# Python requirements for nf-core plugin
# This file is for pip/uv users who prefer Python package managers over conda

# nf-core tools (Python package)
nf-core>=3.5.0

# Note: nf-test and Nextflow are NOT available via pip
# They must be installed separately:
#
# nf-test: Download from https://github.com/askimed/nf-test/releases
#   curl -fsSL https://get.nf-test.com | bash
#
# Nextflow: Download from https://nextflow.io
#   curl -s https://get.nextflow.io | bash
#
# Or use conda/mamba for all-in-one installation:
#   mamba env create -f environment.yml
